export function processPackingList(shipmentId: string, items: any[]) {
  // stub: normalize item weights, dimensions
  return { shipmentId, itemCount: items.length };
}

export default processPackingList;
